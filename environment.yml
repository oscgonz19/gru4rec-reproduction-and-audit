name: gru4rec-study
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pytorch
  - cpuonly
  - numpy>=1.24,<2.0
  - pandas>=2.0
  - matplotlib>=3.7
  - optuna>=3.0
  - pytest>=7.0
  - joblib>=1.0
  - pip
